<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="vm"
            type="br.com.involves.viewluck.viewmodel.MultiChoiceViewModel" />

        <variable
            name="adapter"
            type="br.com.involves.viewluck.viewmodel.MultiChoiceBindAdapterVM" />

        <variable
            name="actions"
            type="br.com.involves.viewluck.viewmodel.MultiChoiceActions" />
    </data>

    <android.support.v7.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="2dp"
        android:orientation="vertical">

        <android.support.v7.widget.AppCompatTextView
            android:id="@+id/txt_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{vm.fieldLabel}"
            android:textSize="14sp"
            android:textStyle="bold" />

        <android.support.v7.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layoutManager="@{vm.getLayoutManager()}"
            app:options="@{vm.fieldModel}"
            tools:listitem="@layout/viewluck_selection_row_multi" />

        <!--<br.com.involves.viewluck.view.NonScrollableGridView-->
        <!--android:id="@+id/gd_multiple_choice"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="match_parent"-->
        <!--android:choiceMode="multipleChoice"-->
        <!--app:options="@{vm.objectList}"-->
        <!--app:OnItemClickListener="@{(parent, view, position,id)->actions.onItemClick(view, position, vm.answer)}"-->
        <!--android:numColumns="auto_fit" />-->

    </android.support.v7.widget.LinearLayoutCompat>
</layout>